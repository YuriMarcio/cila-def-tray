declare type ApiErrorParams = {
    code: string;
    message: string;
    details?: string;
    data?: object;
};
export declare class ApiError extends Error {
    code: string;
    details?: string;
    data?: object;
    constructor({ code, message, details, data }: ApiErrorParams);
    toString(): string;
}
export {};
